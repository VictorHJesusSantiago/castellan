spring:
  application:
    name: castellan-apm-collector
  datasource:
    url: jdbc:h2:mem:castellan-apm;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password: ""
  flyway:
    locations: classpath:db/migration

server:
  port: 8081
