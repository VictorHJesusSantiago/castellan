spring:
  application:
    name: castellan-ledger-api
  datasource:
    url: jdbc:h2:mem:castellan;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password: ""
  flyway:
    locations: classpath:db/migration

server:
  port: 8080
